// (A)

function setup() {
  createCanvas(340, 340);
}

function draw() {
 line(170,170,random(width), random(height));
}

// (B)

function setup() {
  createCanvas(340, 340);
}

function draw() {
 line(random(width), random(height),340,0);
}

// (C)

function setup() {
  createCanvas(340, 340);
}

function draw() {
 line(40,120,80,60);
}

// (D)
?
